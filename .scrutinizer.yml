filter:
    excluded_paths:
        - 'js/tests/*'
        - 'js/vendor/*'
        - 'vendor/*'
        - 'l10n/*'
        - 'tests/*'

imports:
    - javascript
    - php

tools:
    external_code_coverage:
        timeout: 7200 # Timeout in seconds. 120 minutes
        runs: 6
